name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  packages: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup tooling
        uses: ./.github/actions/ci-setup
      - name: Run fmt
        run: cargo fmt --all --check
  clippy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup tooling
        uses: ./.github/actions/ci-setup
      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
  test:
    runs-on: ubuntu-latest
    needs: [fmt, clippy]
    env:
      GREENTIC_CONTROL_CHAIN_PATH: vendor/greentic-control-chain
      CONTROL_CHAIN_GH_TOKEN: ${{ secrets.GREENTIC_GITHUB_TOKEN != '' && secrets.GREENTIC_GITHUB_TOKEN || github.token }}
      GREENTIC_FAST2FLOW_GH_TOKEN: ${{ secrets.GREENTIC_BIZ_GH_TOKEN != '' && secrets.GREENTIC_BIZ_GH_TOKEN || github.token }}
      GREENTIC_GITHUB_TOKEN: ${{ secrets.GREENTIC_GITHUB_TOKEN }}
      GREENTIC_GITHUB_USER: ${{ vars.GREENTIC_GITHUB_USER }}
      GREENTIC_GITHUB_ORG: ${{ vars.GREENTIC_GITHUB_ORG }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checkout greentic-control-chain fixtures
        uses: actions/checkout@v4
        with:
          repository: greentic-ai-org/greentic-control-chain
          path: vendor/greentic-control-chain
          token: ${{ env.CONTROL_CHAIN_GH_TOKEN }}
      - name: Setup tooling
        uses: ./.github/actions/ci-setup
      - name: GHCR login
        uses: docker/login-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ env.GHCR_TOKEN != '' && env.GHCR_TOKEN || env.GITHUB_TOKEN }}
      - name: Fetch greentic-fast2flow release binaries
        env:
          GH_TOKEN: ${{ env.GREENTIC_FAST2FLOW_GH_TOKEN }}
          GREENTIC_FAST2FLOW_REQUIRE_GTPACK: "1"
        run: ./scripts/fetch_fast2flow_release.sh
      - name: Build ingress-control fixture packs
        env:
          GREENTIC_FAST2FLOW_GTPACK: artifacts/fast2flow-release/latest/fast2flow.gtpack
        run: ./scripts/build_e2e_fixtures.sh
      - name: Generate pack audit index
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
          GITHUB_ACTOR: ${{ github.actor }}
          GT_CRANE_LOGIN: "0"
        run: cargo run -p greentic-integration --bin pack_audit
      - name: Run tests
        run: cargo test --all-features
  gtest:
    runs-on: ubuntu-latest
    needs: [fmt, clippy]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup tooling
        uses: ./.github/actions/ci-setup
      - name: Install greentic CLIs
        run: |
          cargo binstall greentic-pack --no-confirm --force
          cargo binstall greentic-component --no-confirm --force
          cargo binstall greentic-flow --no-confirm --force
          cargo binstall greentic-runner --no-confirm --force
      - name: Run gtest smoke scripts
        run: |
          cargo run -p greentic-integration-tester -- --test tests/gtests/00_smoke_validator.gtest
          cargo run -p greentic-integration-tester -- --test tests/gtests/01_capture_and_expect.gtest
          cargo run -p greentic-integration-tester -- --test tests/gtests/echo-pack-doctor.gtest
  publish:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: [fmt, clippy]
    env:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.91.0
      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
      - name: Publish crates
        if: env.CARGO_REGISTRY_TOKEN != ''
        run: |
          publish() {
            local crate="$1"
            local output
            set +e
            output=$(cargo publish -p "$crate" --token "$CARGO_REGISTRY_TOKEN" 2>&1)
            local status=$?
            set -e
            if [ "$status" -ne 0 ]; then
              if echo "$output" | grep -qi "already exists"; then
                echo "$output"
                echo "Skipping $crate; already published."
                return 0
              fi
              echo "$output"
              return "$status"
            fi
            echo "$output"
          }
          publish greentic-integration-core
          publish greentic-integration-validator
          publish greentic-integration-tester
          publish runner-smoke
          publish greentic-integration
  release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ${{ matrix.os }}
    needs: [fmt, clippy]
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            ext: tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            ext: zip
          - os: macos-15
            target: aarch64-apple-darwin
            ext: tar.gz
          - os: macos-15
            target: x86_64-apple-darwin
            ext: tar.gz
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.91.0
      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
      - name: Install target
        shell: bash
        run: rustup target add "${{ matrix.target }}"
      - name: Read version
        id: version
        shell: bash
        run: |
          version=$(python - <<'PY'
          import tomllib
          from pathlib import Path
          data = tomllib.loads(Path("Cargo.toml").read_text())
          print(data["workspace"]["package"]["version"])
          PY
          )
          echo "version=${version}" >> "$GITHUB_OUTPUT"
      - name: Build release binaries
        shell: bash
        run: |
          cargo build --release --target "${{ matrix.target }}" \
            -p greentic-integration \
            -p greentic-integration-tester \
            -p greentic-integration-validator \
            -p runner-smoke
      - name: Package artifacts (unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          version="${{ steps.version.outputs.version }}"
          target="${{ matrix.target }}"
          bin_dir="target/${target}/release"
          mkdir -p dist
          package() {
            name="$1"
            shift
            dir="dist/${name}-v${version}-${target}"
            mkdir -p "$dir"
            for bin in "$@"; do
              cp "${bin_dir}/${bin}" "$dir/"
            done
            tar -czf "dist/${name}-v${version}-${target}.tar.gz" -C dist "$(basename "$dir")"
            rm -rf "$dir"
          }
          package greentic-integration greentic-integration pack_audit greentic-integration-tester
          package greentic-integration-tester greentic-integration-tester
          package greentic-integration-validator greentic-integration-validator
          package runner-smoke runner-smoke
      - name: Package artifacts (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $target = "${{ matrix.target }}"
          $binDir = "target/$target/release"
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          function Package($name, $bins) {
            $dir = "dist/$name-v$version-$target"
            New-Item -ItemType Directory -Force -Path $dir | Out-Null
            foreach ($bin in $bins) {
              Copy-Item "$binDir/$bin.exe" "$dir/"
            }
            Compress-Archive -Path "$dir/*" -DestinationPath "dist/$name-v$version-$target.zip"
            Remove-Item -Recurse -Force $dir
          }
          Package "greentic-integration" @("greentic-integration","pack_audit","greentic-integration-tester")
          Package "greentic-integration-tester" @("greentic-integration-tester")
          Package "greentic-integration-validator" @("greentic-integration-validator")
          Package "runner-smoke" @("runner-smoke")
      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: v${{ steps.version.outputs.version }}
          files: dist/*.${{ matrix.ext }}
